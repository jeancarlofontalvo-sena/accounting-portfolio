function sliceUntilCondition(data, startIndex, stopCondition) {
  if( typeof LodashGS !== "undefined" ) {
    
    var items = [];
  
    do {        
      record.counts.push( data[ i ] );
      i++;
    }
    while( stopCondition( rawValues[ i ] ) );
  }
  
  throw "Se necesita de Lodash";
      
}
